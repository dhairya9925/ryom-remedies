[install]
# 24h supply-chain guard: skip package versions published less than a day ago.
minimumReleaseAge = 86400
# Each entry bypasses the 24h guard for one package. Keep this empty unless a
# reviewed dependency needs to be installed immediately after publication.
minimumReleaseAgeExcludes = []
